#include <iostream>
using namespace std;


auto search(int a[], int n, int needle) -> int {
    int index = -1;
    for (int i = 0; i < n; i++) {
        if (needle==a[i]){
            index = i;
            break;
        }
    }
    return index;
}

    auto main(int argc, char **argv) -> int {
        int tab[100];
        int n = 10;
        int a[] = {42, 9, -1, 18, 59, 3, 101, 31, 72, 12};
        int const c= stoi(argv[1]);
        cout << search(a,n,c) <<"\n";
    return 0;
}#include <iostream>
using namespace std;


auto search(int a[], int n, int needle) -> int {
    int index = -1;
    for (int i = 0; i < n; i++) {
        if (needle==a[i]){
            index = i;
            break;
        }
    }
    return index;
}

    auto main(int argc, char **argv) -> int {
        int tab[100];
        int n = 10;
        int a[] = {42, 9, -1, 18, 59, 3, 101, 31, 72, 12};
        int const c= stoi(argv[1]);
        cout << search(a,n,c) <<"\n";
    return 0;
}
